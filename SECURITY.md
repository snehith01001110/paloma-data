# Security policy

Report vulnerabilities through GitHub's private vulnerability reporting for this repository. Do
not include credentials, provider payloads, user data, or exploitable details in a public issue.

The supported version is the current `main` branch and the deployed production revision. Rotate
any credential immediately if it may have been exposed, then report the incident privately.
